name: Workflows AI Review (SCW)

on:
  workflow_dispatch:
    inputs:
      mode:
        description: "What to review"
        type: choice
        required: true
        default: "summary-only"
        options:
          - summary-only
          - workflows-state
          - full

permissions:
  contents: read
  pull-requests: write

jobs:
  ai_review:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Show context
        run: |
          echo "AI Review placeholder running in repo: $GITHUB_REPOSITORY"
          echo "Mode: ${{ github.event.inputs.mode }}"
          echo "Commit: $GITHUB_SHA"

      # NOTE: This is a safe placeholder step. Once this is running reliably
      # and the Run button is visible, we can wire in real AI logic here.
      - name: Placeholder AI Review
        run: |
          echo "----------------------------------------"
          echo " StegVerse Labs :: SCW :: AI Review Stub"
          echo "----------------------------------------"
          echo "This workflow is VALID and runnable."
          echo "Next step is to plug in real AI review logic."
          echo ""
          echo "You can confirm by:"
          echo "  • Opening the Actions tab"
          echo "  • Selecting 'Workflows AI Review (SCW)'"
          echo "  • Clicking 'Run workflow' (pick a mode)"
          echo ""
          echo "Once confirmed, we'll upgrade this step into"
          echo "a full AI reviewer that reads .steg/state/*,"
          echo "checks workflows, and opens Issues/PR comments."
