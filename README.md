# StegVerse — Sovereign Control Workspace (SCW)

This repo is the central orchestrator for StegVerse automation.

## v2 Patch Notes (important)
This bundle fixes three blockers you hit:
1. **`scw/requirements.txt` missing** → now included.
2. **Workflow calling SCW without args** → orchestrator passes `command` + `target_repo`.
3. **`No GitHub token found`** → workflow maps org/repo secret to `GH_TOKEN` safely.

## Required secret
Set **org-level** secret (preferred):
- `GH_STEGVERSE_AI_TOKEN` (fine‑grained PAT with access to StegVerse‑Labs repos)

Optional fallbacks:
- `GH_STEGVERSE_PAT` (classic PAT)
- If neither exist, workflow uses `github.token` (works for same‑repo ops, not cross‑repo).

## Run
GitHub → Actions → **StegVerse SCW Orchestrator** → Run workflow.

Recommended first run:
- command: `self-test`
- target_repo: `StegVerse-Labs/TVC`

If self-test passes, run:
- command: `autopatch`
- target_repo: any repo you want standardized.

---
Generated by SCW full bundle v2.
